import { Session } from "next-auth";
const MyDataPage = () => {
  return (
    <div className="h-[90vh] bg-purple-100 flex justify-center items-center text-black">
      Esta es la pagina donde se van a mostrar los datos del user
    </div>
  );
};

export default MyDataPage;
